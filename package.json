{
  "name": "vi-slides-lite",
  "version": "1.0.0",
  "description": "Vi-Slides Lite — Real-time classroom engagement platform",
  "scripts": {
    "dev": "concurrently --names \"server,client\" --prefix-colors \"cyan,magenta\" \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "db:push": "npm run db:push --prefix server"
  },
  "devDependencies": {
    "@types/axios": "^0.9.36",
    "autoprefixer": "^10.4.27",
    "concurrently": "^9.2.1",
    "postcss": "^8.5.8",
    "tailwindcss": "^4.2.2"
  },
  "dependencies": {
    "axios": "^1.14.0"
  }
}
